import type {Icons} from './types'
import moonIcon from '../ui/moon-icon'
import sunIcon from '../ui/sun-icon'
import playIcon from '../ui/play-icon'
import pauseIcon from '../ui/pause-icon'
import chartIcon from '../ui/chart-icon'
import resetIcon from '../ui/reset-icon'
import shuffleIcon from '../ui/shuffle-icon'
import tableIcon from '../ui/table-icon'

export const icons: Icons = {
  moon: moonIcon,
  sun: sunIcon,
  play: playIcon,
  pause: pauseIcon,
  chart: chartIcon,
  reset: resetIcon,
  shuffle: shuffleIcon,
  table: tableIcon
}
